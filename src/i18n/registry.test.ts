// Skipped: This test covers the old Lit-based UI i18n registry (ui/src/i18n/lib/registry.ts)
// which was replaced by the React UI. Remove this file once the React UI has its own i18n tests.
import { describe, it } from "vitest";

describe.skip("ui i18n locale registry (old Lit UI — replaced by React UI)", () => {
  it("placeholder", () => {});
});
